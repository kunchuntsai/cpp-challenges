// Challenge 2: Implement a simple smart pointer
// Create a basic smart pointer class that manages memory allocation and deallocation.
// It should support the following operations:
// - Constructor that takes a raw pointer
// - Destructor that deletes the managed object
// - Overload the -> and * operators
// - Implement move semantics (move constructor and move assignment operator)

template <typename T>
class SmartPtr {
    // TODO: Implement the smart pointer class
};
